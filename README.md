# Personal CRM with FastAPI & Supabase

## Setup Instructions

1. Create `.env` with your Supabase, Twilio, and OpenAI credentials (see `.env.example`).
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
